const express = require('express');

const { body } = require('express-validator');

const router = express.Router();

// const authControllers = require('')

// router.get('/signup', linkControllers.getLinks);

module.exports = router;